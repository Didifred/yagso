[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yagso"
version = "0.1.0"
description = "A Python CLI utility"
authors = [{name = "Your Name", email = "your.email@example.com"}]
dependencies = []

[project.scripts]
yagso = "yagso.cli:main"